Claude Opus 5 (1M context) (high)

# Handover: mousiki - 2026-09-14 17:00

## Current Task
Refining the mousiki TUI music player (Johan's fork `johan--/mousiki`), four agreed work items, then a macOS test.

## Status
- [x] Item 1 (fixes) implemented and verified, uncommitted
- [ ] Item 2 (albums) - brainstorm with Johan first, no design yet
- [ ] Item 3 (configurable keys) - approved in principle, short design needs a yes before coding
- [ ] Item 4 (Page Up/Down, Home/End) - approved in principle, short design needs a yes before coding
- [ ] macOS test after all four
- Current branch: `sandbox`
- Last commit: `4d44552` - Update README with default keybindings section

## Immediate Next Steps
1. Ask Johan whether to commit item 1 (no project CLAUDE.md, so no standing git permission). Suggested one-liner: `Fix end-of-list replay, uppercase extensions, music path fallback, README keys`
2. Present the short in-chat design for item 3 and wait for a yes
3. Present the short in-chat design for item 4 and wait for a yes
4. Brainstorm item 2 (albums) with Johan: architectural path, spec file under `docs/superpowers/specs/`
5. Plan the macOS test (Ivy Bridge Mac, separate machine; check how to reach it and whether miniaudio/ffmpeg/yt-dlp build there)

## Blockers / Open Questions
- Git: nothing committed. `setup.sh` changes predate this session (not Johan's session work here); confirm whether they belong in a commit
- macOS access method not yet discussed

## Key Decisions Made
- Show Johan the whole option list at once, never a single-pick question that hides items
- Item 1 scope (done):
  - `.gitignore` with `build/`
  - `play_relative` (`src/app.cpp:800`) does nothing past either end of the list, instead of clamping and replaying the last track forever
  - `is_audio_file` (`src/local_source.cpp:9`) lowercases the extension
  - `scan` falls back to `~/Music` and `~/disk/Music` when no configured path exists (was: only when none configured)
  - README key table rewritten to match the hardcoded handler
- Ship risk (item 1): confidence high. Verified by a scratch test of `LocalSource::scan` (uppercase, fallback, mixed: 2/1/2) and a tmux end-to-end run (last track played about 3 s, then PLAY and "no track loaded", no replay). Possible surprise: `n` on the last row and `b` on the first now do nothing. Blast radius stayed in the planned files.

## Context the Next Session Needs
- Key handling is hardcoded in `App::handle_key` (`src/app.cpp:1155`). `resolve_hotkey_action` (`src/app.cpp:374`) is never called, so `HKey...` config lines do nothing. Default hotkey map in `src/settings.cpp:280`
- Terminal input: `TerminalIO::poll_key` (`src/terminal_ui.cpp:55`) reads only 2 bytes after ESC, and decodes arrows only. Page Up/Down (`ESC [ 5 ~`) need a longer read. Left arrow and Shift+D collide (both return `'D'`)
- Albums context: no album concept anywhere. Scan order is unsorted filesystem order. Search matches title and artist only (`src/app.cpp:402`). `LocalTrack` has `title`, `path`, `folder_artist`. `probe_row_meta` reads only duration and artist tags. `.m3u` files sit in album folders but are ignored. Queue always plays before PlaybackMode (`advance_track`, `src/app.cpp:822`)
- README lists keys that do not exist: `m`, `f`, `c`, `y` (now removed from README)
- Twin found, not fixed: `src/native_duration.cpp:172` compares extensions case-sensitively, so uppercase `.MP3`/`.FLAC` skip the native duration parser and fall back to ffprobe
- `app.cpp` is 2,338 lines, a candidate for splitting if items 2-4 grow it
- Build: `cmake --build build --parallel "$(nproc)"`. No test suite in repo
- End-to-end test pattern: tmux session with `HOME` pointed at a scratch home holding `.config/mousiki/config.txt` with one `LocalMusicPath`, generate short tones with ffmpeg, drive with `tmux send-keys`, read with `tmux capture-pane`
- Johan's live config `~/.config/mousiki/config.txt` still holds the Termux paths; he added `~/Music` himself (library shows 1,600+ tracks)

## Running Processes
- None (tmux test session `mtest` killed)
