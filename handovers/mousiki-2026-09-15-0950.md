Claude Opus 5 (1M context) (high)

# Handover: mousiki - 2026-09-15 09:50

## Current Task
Refining the mousiki TUI music player (fork `johan--/mousiki`). This session shipped configurable hotkeys, queue-aware next/previous, Show toggles, the settings tab highlight, and About credits. Albums and playlists is next.

## Status
- [x] Item 1 (fixes): committed by Johan in `a4ab454`
- [x] Item 3 (configurable keys), plus the Show rename and the About credits: done and tested, uncommitted
- [x] Next/previous track (A1, B1, C1): done and tested, uncommitted
- [ ] Item 4 (Page Up/Down, Home/End): approved in principle, needs a short design and a yes
- [ ] Albums and playlists: brainstorm started (architectural path), Johan has not picked options yet
- [ ] macOS test after all items
- Current branch: `sandbox`
- Last commit: `a4ab454` - various updates
- Uncommitted: `README.md`, `config.txt`, `src/app.cpp`, `src/app.h`, `src/settings.cpp`, `src/settings.h`, `src/terminal_ui.cpp`, `src/terminal_ui.h`

## Immediate Next Steps
1. Ask Johan to commit the uncommitted work (no project CLAUDE.md, so no git writes). Suggested one-liner: `Configurable hotkeys, queue-aware next/previous with history, Show toggles, About credits`
2. Continue the albums and playlists brainstorm. Candidate list shown to Johan, awaiting his pick: (1) add whole folder to queue, (2) album view, (3) play existing `.m3u` files, (4) save queue as `.m3u`, (5) persist queue across sessions. Item 6 (next/previous follow the queue) is done.
3. Short design for item 4 (Page Up/Down, Home/End). `poll_key` already has the `KeyCode` enum; it needs a longer escape read for `ESC [ 5 ~` style sequences, then new hotkey actions.
4. Offer fixes for the open bugs below, each as a separate item

## Blockers / Open Questions
- Git: nothing from this session is committed
- Save-on-quit clobber (see Context): not fixed, Johan has not chosen when
- macOS access method not yet discussed

## Key Decisions Made
- Hotkeys: one action table, `hotkey_defs()` in `src/settings.h`/`src/settings.cpp`. Order decides clashes (first listed wins). Config lines override defaults per name. Values allow comma lists (`n,N`). Case-sensitive, both cases kept as defaults where they existed.
- `HKeyRemoveHoveringSongFromQueue` defaults to `t`; new `HKeyRemoveLastFromQueue` on `d` (Johan's answer B)
- Arrow keys return `KEY_ARROW_*` codes (1000 and up) from `poll_key`, so Left arrow and Shift+D are distinct (answer C)
- Left arrow still moves the selected queue item down while the queue is focused: kept to avoid an unapproved behavior change
- Unknown `HKey` names (old `HKeyToggleRepeat` and others) are kept on save but do nothing
- Toggle settings renamed `Eliment...` to `Show...` (`ShowDisk` and so on), `Seprator` to `Separator`; old names still load
- Next: queue first, else the row after the playing track. Previous: history of up to 100 played tracks, else the row above. Auto-advance continues from the playing track, not the cursor.
- About text lives only in code (`about_lines()` in `src/app.cpp`), single source of truth. Config no longer stores it; the loader skips old `ClassTextAboutApp` blocks.
- About credits: maintained by qoolpix.music with `instagram.com/qoolpix.music` below; originally created by ender (GitHub itzender5820, repo github.com/itzender5820/mousiki); Apache License 2.0. No fork line (Johan: people already cloned the fork to see it).
- Active settings tab title shown in reverse video, ended with `ESC [27m` so the border color carries on
- Ship risk: confidence high. Verified by tmux end-to-end runs (keys, remaps, clash warning, next/previous sequence a1, a4, a1, a2, a3, a2, tab escape, About text, saved config). Likely first failure: history replay when a track load fails (entry is dropped, no retry). Blast radius: stayed in the files listed above.

## Context the Next Session Needs
- **Save-on-quit clobber:** the player writes the whole config from memory on every quit (`src/app.cpp`, `save_settings` call at the end of `App::run`) and on `s` in Settings. Any hand edit to `~/.config/mousiki/config.txt` while the player runs is undone. Edit the live config only with the player closed, or through the Settings screen.
- `pgrep -f build/mousiki` inside a Bash call matches the calling shell itself; use `ps -eo pid,lstart,args | grep "[b]uild/mousiki"` instead
- Johan's live config was fixed this session (`t` for remove-hovering, `s,S`, `n,N`, `p,P`, `q,Q`), backups in the old session scratchpad only
- Open bugs, not fixed:
  - List duration column shows `47...` for many FLAC rows while the playing track shows the right length; suspect the native duration parser
  - `src/native_duration.cpp` compares extensions case-sensitively, so uppercase `.MP3`/`.FLAC` fall back to ffprobe
- Albums context: no album concept. `LocalTrack` has `title`, `path`, `folder_artist`. Search matches title and artist only. `.m3u` files in album folders are ignored. Queue and history are in memory only.
- Queue and history code: `play_next`, `play_previous`, `play_from_queue`, `start_queue_item`, `playing_row`, `advance_track` in `src/app.cpp`; history recorded in `poll_pending_load`
- Build: `cmake --build build --parallel "$(nproc)"`. No test suite.
- End-to-end test pattern: tmux session with `HOME` pointed at a scratch home holding `.config/mousiki/config.txt` with one `LocalMusicPath`; generate short tones with ffmpeg (4 s tones test auto-advance); drive with `tmux send-keys`; read the playing track with `tmux capture-pane -p | grep -o "Name *: [a-z0-9]*"`; check escapes with `capture-pane -e`. Use a 60-row window, or the status line falls off screen.
- `app.cpp` is about 2,500 lines, a candidate for splitting (hotkeys, queue/history, settings screen)
- `README.md` clone URL now points to `johan--/mousiki`; Credits section added

## Running Processes
- None started by this session (tmux test session `mtest` exited). Johan may have his own player open; it needs a restart to pick up the new build.
